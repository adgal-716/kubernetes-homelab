apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-info
  namespace: geoserver
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 400Gi
      

